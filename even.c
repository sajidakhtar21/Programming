#include<stdio.h>
int main()
{
    int i,n;
    printf("Enter the Number :\n");
    scanf("%d",&n);
    for(i=0;i<=n;i++)
    {
        if(i%2==0)
        {
            printf("Even no : %d",);
            break;
        }
        else
        {
            printf("Odd No :%d",i);
        }
    }
}